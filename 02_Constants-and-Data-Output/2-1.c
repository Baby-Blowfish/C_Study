/**
 * @file 2-1.c
 * @brief A simple program to add two integers.
 *
 * This program calculates the sum of 10 and 20 and stores the result
 * in a variable. It demonstrates basic arithmetic operations in C.
 *
 * @author 김효진
 * @date 2024-11-19
 */

#include <stdio.h>

/**
 * @brief Main function
 *
 * The entry point of the program. It calculates the sum of 10 and 20
 * and stores it in the variable `result`.
 *
 * @return int Returns 0 to indicate successful program termination.
 */
int main(void)
{
    int result = 10 + 20; /**< Stores the sum of 10 and 20. */
    return 0;             /**< Indicates successful program termination. */
}

/**
 * @example
 * ### Execution Example:
 * No input/output is performed in this program. It simply calculates
 * and stores the result in a variable.
 *
 * @details
 * - The variable `result` holds the value `30` but is not printed.
 * - The program exits successfully by returning 0.
 */

